#include <stdlib.h>
#include <stdio.h>
// El ejecutable de este programa se tiene que
// llamar como especificado en el programa4!
/* Función principal */
int main(void)
{
 int i=0;
 /* Introducción por teclado del número de veces que se repetirá el
bucle while */
 for(i=0;i<10;i++)
printf("Hola soy el nuevo proceso hijo !\n");


 exit(EXIT_SUCCESS);
}
